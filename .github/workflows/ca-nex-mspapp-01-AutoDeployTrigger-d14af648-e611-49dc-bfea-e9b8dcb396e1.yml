name: Trigger auto deployment for ca-nex-mspapp-01

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/ca-nex-mspapp-01-AutoDeployTrigger-d14af648-e611-49dc-bfea-e9b8dcb396e1.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.CANEXMSPAPP01_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CANEXMSPAPP01_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CANEXMSPAPP01_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: 
          registryUsername: ${{ secrets.CANEXMSPAPP01_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CANEXMSPAPP01_REGISTRY_PASSWORD }}
          containerAppName: ca-nex-mspapp-01
          resourceGroup: rg-nex-test-play-uks-001
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


